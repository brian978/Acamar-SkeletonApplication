<?php
/** @var \Application\Model\Book $book */
?>

<div>
    <a href="<?= $this->url('mvc', array('controller' => 'index', 'action' => 'add')); ?>" class="btn btn-primary">
        Add new book
    </a>
</div>
<br>
<table class="table">
    <tr>
        <th>Title</th>
        <th>ISBN</th>
        <th>Publisher</th>
        <th>Author</th>
        <th>Options</th>
    </tr>
    <?php foreach ($this->books as $book): ?>
        <tr>
            <td><?= $book->getTitle() ?></td>
            <td><?= $book->getIsbn() ?></td>
            <td><?= $book->getPublisher()->getName() ?></td>
            <td><?= $book->getAuthor() ?></td>
            <td>
                <span class="btn-xs btn-primary glyphicon glyphicon-pencil"></span>
                <span class="btn-xs btn-primary glyphicon glyphicon-trash"></span>
            </td>
        </tr>
    <?php endforeach ?>
</table>

